// lib/features/terapis/terapis_rpt.dart
import 'package:flutter/material.dart';

class TerapisLaporanCreate extends StatelessWidget {
  const TerapisLaporanCreate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Halaman Rancangan Pembelajaran Tahunan',
        style: TextStyle(
          fontSize: 20,
          fontWeight: FontWeight.bold,
          color: Color(0xFF1A1E25),
        ),
      ),
    );
  }
}
